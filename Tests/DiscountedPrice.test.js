import { discountedPrice } from './index.html';

describe('DiscountedPrice', () => {
  it('should return the discounted price', () => {
    // Detta är själva testet
    expect(discountedPrice).toBe(2);
  });
});
